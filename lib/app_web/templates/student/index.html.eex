<div class="sub-container">
<h2 align="center">Listing Students</h2>

<div class="school-nav">
  <%= link "New Student", to: Routes.student_path(@conn, :new) %>
  <%= link "Quiz and Exam", to: Routes.quiz_path(@conn, :index) %>
  <%= link "Student Attendance", to: Routes.attendance_path(@conn, :search_attendance) %>
</div>

<br>
<%= if get_flash(@conn, :info) do %>
  <div class="alert alert-info">
    <p><%= get_flash(@conn, :info) %></p>
  </div>
<% end %>

<%= if get_flash(@conn, :error) do %>
  <div class="alert alert-danger">
    <p><%= get_flash(@conn, :error) %></p>
  </div>
<% end %>

<table>
  <thead>
    <tr>
      <th>Firstname</th>
      <th>Lastname</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for student <- @students do %>
    <tr>
      <td><%= student.firstname %></b></td>
      <td><%= student.lastname %></td>

      <td>
        <span><%= link "Edit Profile", to: Routes.student_path(@conn, :edit, student), class: "td-blue" %></span>
        <span><%= link "Delete Account", to: Routes.student_path(@conn, :delete, student), method: :delete, class: "td-red", data: [confirm: "Are you sure?"] %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>
</div>
